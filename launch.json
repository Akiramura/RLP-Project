{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Tauri Rust",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/rlp-project.exe",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}